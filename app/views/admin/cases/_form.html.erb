<%= semantic_form_for [:admin, @case] do |f| %>
    <%= f.inputs :title, :facts %>
     <%= f.inputs :purpose_id, :character_id, :use_id, :nature_id, :amount_id, :financial_id, :for => :test_answer, :name => "ay habal" %>
    <%= f.actions %>
  <% end %>